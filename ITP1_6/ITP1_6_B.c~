#include<stdio.h>
int main(){
  int n , i , s;
  char c;
  int date[4][13] = {0};
  scanf("%d", n);
  for(i = 1 ; i <= n ; i++){
    scanf("%c %d", &c, &s); //S=0 H=1 C=2 D=3
    if(c == 'S'){
      date[0][s] = 1;
    }
    else if(c == 'H'){
      date[1][s] = 1;
    }
    else if(c == 'C'){
      date[2][s] = 1;
    }
    else if(c == 'D'){
      date[3][s] = 1;
    }
  }
  for(i = 1 ; i <= 52-n ; i++){
    
  }
}
